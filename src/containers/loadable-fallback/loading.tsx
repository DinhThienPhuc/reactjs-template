/* CONTAINER: LOADABLE FALLBACK LOADING
   ========================================================================== */

const Loading = () => {
  return <div>Loading...</div>;
};

export default Loading;
