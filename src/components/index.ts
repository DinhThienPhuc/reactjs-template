/* COMPONENT ENTRY
   ========================================================================== */

import Select from "./select/select";
import TextField from "./text-field/text-field";

export default {
  TextField,
  Select,
};
